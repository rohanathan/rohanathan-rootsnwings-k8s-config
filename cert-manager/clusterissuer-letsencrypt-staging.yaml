apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-staging
spec:
  acme:
    # ACME staging endpoint (high rate limits, untrusted certs)
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: rohanathan.s@hotmail.com
    privateKeySecretRef:
      name: letsencrypt-staging-account-key
    solvers:
      - dns01:
          cloudDNS:
            project: rootsnwings-465610
            hostedZoneName: cloudsofglasgow
